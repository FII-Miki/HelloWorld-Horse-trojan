HelloWorld-Horse-trojan
new trojan repo
Easy python3 trojan which give you a ngrok or clodflared link,
you can mask the URL to make it undetectable. When target
open the link, start open the default browser with non-stop.
Please make sure no use it on tiktok/instagram/discord
use it on Whatsapp or any massages services.

installation:
sudo apt update
sudo apt install git
sudo apt isntall python3
git clone (the repo link) in (code)
python3 new.py

Pure evil, MIKI.
